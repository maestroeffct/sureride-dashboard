import ComingSoon from "@/src/components/admin/ComingSoon";

export default function FinanceFinesPage() {
  return (
    <ComingSoon
      title="Fines"
      description="Penalties applied to customers (late return, damage, traffic) and providers (SLA breach). Coming next."
    />
  );
}
